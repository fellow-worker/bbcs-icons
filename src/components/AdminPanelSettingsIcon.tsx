import { AdminPanelSettings } from '@styled-icons/material-rounded/AdminPanelSettings';
import { IconProps } from '../interfaces/IconsProps';

const Icon = ({size} : IconProps) => {
    return <AdminPanelSettings size={size} />
}

export default Icon;