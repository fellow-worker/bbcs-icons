import { WbSunny } from '@styled-icons/material-rounded/WbSunny'
import { IconProps } from '../interfaces/IconsProps';

const DayIcon = ({size} : IconProps) => {
    return <WbSunny size={size} />
}

export default DayIcon;