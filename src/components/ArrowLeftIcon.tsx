import { ArrowBack } from '@styled-icons/material/ArrowBack'
import { IconProps } from '../interfaces/IconsProps';

const Icon = ({size} : IconProps) => {
    return <ArrowBack size={size} />
}

export default Icon;