import { Help } from '@styled-icons/material-rounded/Help'
import { IconProps } from '../interfaces/IconsProps';

const Icon = ({size} : IconProps) => {
    return <Help size={size} />
}

export default Icon;