import { FormatAlignRight } from '@styled-icons/material/FormatAlignRight';
import { IconProps } from '../interfaces/IconsProps';

const Icon = ({size} : IconProps) => {
    return <FormatAlignRight size={size} />
}

export default Icon;